<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             xmlns:local="clr-namespace:VisualChallenge"
             x:Class="VisualChallenge.SearchPage" BackgroundColor="#E3E3E3" Shell.NavBarIsVisible="False">
    <ScrollView Margin="0,0,0,1.3" BackgroundColor="White">
        <FlexLayout Direction="Column" Padding="0,0,0,10">
            <Frame Padding="0" BorderColor="#E3E3E3" HasShadow="True">
                <SearchBar Placeholder="Search" PlaceholderColor="#8E8E93" VerticalOptions="Start" BackgroundColor="#F1F1F1" Margin="10,7" local:BorderlessEffect.HasBorder="False">
                </SearchBar>
            </Frame>
            <StackLayout Orientation="Horizontal" Margin="13,10">
                <StackLayout Orientation="Horizontal" HorizontalOptions="Start">
                    <Label Text="Contacts" TextColor="Black" FontSize="16" FontAttributes="Bold" VerticalTextAlignment="Center"></Label>
                    <Label Text=">" TextColor="#0072C6" FontSize="30" VerticalTextAlignment="Center"></Label>
                </StackLayout>

                <StackLayout Orientation="Horizontal" HorizontalOptions="EndAndExpand">
                    <Label Text="Create" TextColor="Black" FontSize="16" FontAttributes="Bold" VerticalTextAlignment="Center"></Label>
                    <Label Text="+" TextColor="#0072C6" FontSize="30" VerticalTextAlignment="Center"></Label>
                </StackLayout>
            </StackLayout>
            <CollectionView x:Name="clVwPeople" HeightRequest="120" ItemSizingStrategy="MeasureFirstItem">
                <CollectionView.ItemsLayout>
                    <ListItemsLayout>
                        <x:Arguments>
                            <ItemsLayoutOrientation>Horizontal</ItemsLayoutOrientation>
                        </x:Arguments>
                    </ListItemsLayout>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="10,0" Spacing="5">
                            <controls:CircleImage WidthRequest="75" HeightRequest="75" Aspect="AspectFill" Source="{Binding Avatar}"></controls:CircleImage>

                            <Label HorizontalTextAlignment="Center">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="{Binding FirstName}" TextColor="Black"></Span>
                                        <Span Text="&#10;"></Span>
                                        <Span Text="{Binding LastName}" TextColor="#949498"></Span>
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>

                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <StackLayout Orientation="Horizontal" HorizontalOptions="Start" Margin="13,10">
                <Label Text="Upcoming" TextColor="Black" FontSize="16" FontAttributes="Bold" VerticalTextAlignment="Center"></Label>
            </StackLayout>

            <CollectionView x:Name="clVwUpcoming" HeightRequest="170" ItemSizingStrategy="MeasureFirstItem">
                <CollectionView.ItemsLayout>
                    <ListItemsLayout>
                        <x:Arguments>
                            <ItemsLayoutOrientation>Horizontal</ItemsLayoutOrientation>
                        </x:Arguments>
                    </ListItemsLayout>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <ContentView Padding="10" WidthRequest="270">
                            <Frame Padding="0" BorderColor="#E3E3E3" CornerRadius="5" HasShadow="False">
                                <StackLayout>
                                    <StackLayout Padding="10,5" BackgroundColor="#0072C6" Orientation="Horizontal" HorizontalOptions="FillAndExpand" Spacing="12">

                                        <Grid Padding="5">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="50"></RowDefinition>
                                            </Grid.RowDefinitions>
                                            <BoxView Color="White" HeightRequest="50" WidthRequest="50" CornerRadius="25"></BoxView>
                                            <Image Source="airplane.png" Scale="0.5"></Image>
                                        </Grid>

                                        <Label VerticalTextAlignment="Center">
                                            <Label.FormattedText>
                                                <FormattedString>
                                                    <Span Text="{Binding Destination, StringFormat='{}Flight To {0}'}" TextColor="White"></Span>
                                                    <Span Text="&#10;"></Span>
                                                    <Span Text="{Binding Airplane}" TextColor="#ccbdc3c7"></Span>
                                                </FormattedString>
                                            </Label.FormattedText>
                                        </Label>
                                    </StackLayout>

                                    <Grid Padding="10,0" >
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"></RowDefinition>
                                            <RowDefinition Height="Auto"></RowDefinition>
                                            <RowDefinition Height="Auto"></RowDefinition>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="200"></ColumnDefinition>
                                            <ColumnDefinition Width="*"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Label Grid.Row="0" Grid.Column="0" TextColor="Black" LineBreakMode="TailTruncation" FontSize="{OnPlatform iOS='14'}">
                                            <Label.FormattedText>
                                                <FormattedString>
                                                    <Span Text="{Binding AirplaneCode,StringFormat='{}Flight {0} '}"></Span>
                                                    <Span Text="{Binding DepartureAirport}"></Span>
                                                    <Span Text="{Binding ArrivalAirport,StringFormat='{} -> {0}'}"></Span>
                                                </FormattedString>
                                            </Label.FormattedText>
                                        </Label>
                                        <Label Grid.Row="0" Grid.Column="1" Text="In 3h" HorizontalTextAlignment="End" TextColor="#949498" FontSize="{OnPlatform iOS='14'}"></Label>
                                        <Label Grid.Row="1" Grid.Column="0" Text="{Binding DepartureTime,StringFormat='{}From: {0:ddd, MMM dd h:m tt}'}" TextColor="#949498" FontSize="{OnPlatform iOS='14'}" ></Label>
                                        <Label Grid.Row="2" Grid.Column="0" Text="{Binding ArrivalTime,StringFormat='{}To: {0:ddd, MMM dd h:m tt}'}" TextColor="#949498" FontSize="{OnPlatform iOS='14'}"></Label>
                                    </Grid>
                                </StackLayout>
                            </Frame>
                        </ContentView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <StackLayout Orientation="Horizontal" HorizontalOptions="Start" Margin="13,10">
                <Label Text="Files" TextColor="Black" FontSize="16" FontAttributes="Bold" VerticalTextAlignment="Center"></Label>
                <Label Text=">" TextColor="#0072C6" FontSize="30" VerticalTextAlignment="Center"></Label>
            </StackLayout>

            <CollectionView x:Name="clVwFiles" HeightRequest="100" ItemSizingStrategy="MeasureFirstItem">
                <CollectionView.ItemsLayout>
                    <ListItemsLayout>
                        <x:Arguments>
                            <ItemsLayoutOrientation>Horizontal</ItemsLayoutOrientation>
                        </x:Arguments>
                    </ListItemsLayout>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <ContentView Padding="10,0" WidthRequest="150" >
                            <Frame Padding="10" BorderColor="#E3E3E3" CornerRadius="5" HasShadow="False">
                                <StackLayout>
                                    <Image Source="{Binding FileIcon}" WidthRequest="30" HeightRequest="30" HorizontalOptions="Start"></Image>
                                    <Label VerticalOptions="Center">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="{Binding FileName}" TextColor="Black"></Span>
                                                <Span Text="&#10;"></Span>
                                                <Span Text="{Binding FileExt}" TextColor="#949498"></Span>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                </StackLayout>
                            </Frame>
                        </ContentView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </FlexLayout>
    </ScrollView>
</ContentPage>